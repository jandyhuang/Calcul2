main()
{
        f($x) = 2 * x;
        g($x) = 3 * sin(x);

        h($x) = f * g'(x);
        :h;
        :h'(x);
        :h@x(1, 3);

        q($x) = sin(x);
        ##:h(1.57);

        a = 3;
        :a ^ 2;
}
